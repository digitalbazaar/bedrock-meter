const {meter} = require('bedrock-meter');

describe('meters', () => {
  it('setHandler', async () => {
  });

  it('insert', async () => {
  });

  it('update', async () => {
  });

  it('get', async () => {
  });

  it('remove', async () => {
  });

  it('use', async () => {
  });
});
